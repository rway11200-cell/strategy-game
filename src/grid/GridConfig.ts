export {
  CELL_TYPES,
  createGridConfig,
  createGridConfig as createDefaultGridConfig,
  gridToWorld,
  worldToGrid,
} from "../core/grid/GridConfig";

export type { CellCoord, CellType, CellState, GridConfig, Point } from "../core/grid/GridConfig";
